<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Weight</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/styles.css') }}">



    <style>
        /* Base Styles */
        body, html 
        {
            font-family: 'Arial', sans-serif;
            background: #f4f4f4;
            margin: 0;
            padding: 20px;
            color: #333;
        }
       
        .add_weight-container 
        {
            width: 100%;
            background-color: white;
            padding: 30px 40px;
            border-radius: 8px;
            box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.1);
            margin: auto;
            overflow: hidden;
        }

        h2 
        {
            margin-bottom: 25px;
            font-weight: 600;
        }
    
        input[type="number"], input[type="date"] {
            width: 100%;
            padding: 10px;
            margin: 5px 0 15px;
            border: 1px solid #ddd;
            box-sizing: border-box;
            border-radius: 4px;
        }

        input[type=submit] 
        {
            width: 100%;
            background-color: #3682c9;
            color: white;
            padding: 14px 20px;
            margin: 8px 0;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            font-weight: 600;
            transition: background-color 0.3s;
        }

        input[type=submit]:hover 
        {
            background-color: #3682c9;
        }
    </style>
</head>

<body>
    <!-- Navigation Bar -->
    <nav>
        <a href="#" class="logo"> Nutrifit </a>
        <ul>
            <li><a href="{{ url_for('daily_intake')}}">Cancel</a></li>
        </ul>
    </nav>

    <!-- Add Weight Container -->
    <section style="padding: 50px 0; text-align: center;">
        <div class="add_weight-container" style="max-width: 380px; background-color: #fff; padding: 30px 40px; border-radius: 8px; box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05); margin: 0 auto;">
            <h1>New Weight</h1>
            <form action="/add_weight" method="POST" style="text-align: left;">

                <div class="form-group">
                    <label for="weight">New Weight:</label>
                    <input type="number" id="weight" name="weight" required>
                </div>
                <div class="form-group">
                    <label for="date">Date:</label>
                    <input type="date" id="date" name="date" required>
                </div>
                <button type="submit">Add Entry</button>

            </form>
            <table>
                <tr>
                    <th>Date</th>
                    <th>Food Item</th>
                    <th>Calories</th>
                </tr>
                {% for entry in entries %}
                <tr>
                    <td>{{ entry['date'] }}</td>
                    <td>{{ entry['food_item'] }}</td>
                    <td>{{ entry['calories'] }}</td>
                </tr>
                {% endfor %}
            </table>
            
            {% if error %}
                <p style="color: red; font-weight: bold;">{{ error }}</p>
            {% endif %}
        </div>
    </section>
</body>

</html>